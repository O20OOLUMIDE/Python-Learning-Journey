# Max problem

# Subroutine that returns the highest of two numbers
def maximum(num1, num2):
    if num1 > num2:
        return num1
    else:
        return num2

# Main program
num1 = 2
num2 = 3
biggest = maximum(num1, num2)
print("The highest of the two numbers is {}".format(biggest))
